
<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%=image_url("cocktail1") %>);">
  <%= @cocktail.name %>
</div>

  <div class="col-sm-12 col-md-6">
    <table class="table">
      <tbody>
        </tbody><thead>
          <tr><th>Ingredient</th>
          <th>Description</th>
          <th>Delete</th>
        </tr></thead>
          <tbody><tr>
            <% @cocktail.doses.each do |dose| %>
            <td><%= dose.ingredient.name %></td>
            <td><%= dose.description %></td>
            <td><%= link_to dose_path(dose), method: :delete,
      data: { confirm: "Are you sure?" } do %>
            <i class="fa fa-trash destroy_dose"></i>
            <% end %>
      </td>
            <% end %>
            <td>


              </a>
            </td>
          </tr>
        </tbody>
    </table>
    </div>



  <%= link_to "Add a dose",  new_cocktail_dose_path(@cocktail), class: "btn btn-flat"  %>
  <p><%= link_to "See all cocktails", cocktails_path, class: "btn btn-ghost" %></p>

